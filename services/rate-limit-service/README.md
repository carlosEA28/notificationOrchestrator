# Rate Limit Service

This service directory is currently empty. Add the implementation here when ready.

## Suggested Structure

- `cmd/` entrypoints
- `internal/` business logic
- `Dockerfile` and `docker-compose.yml` (optional)

## Notes

- If you want, I can scaffold a minimal service structure for you.

